{
  "name": "aquarium_poc",
  "version": "1.0.0",
  "devDependencies": {
    "turbo": "^2.0.3"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "format": "turbo format",
    "build": "turbo build",
    "start": "cd apps/web && npm run start",
    "postinstall": "./setup.sh"
  },
  "packageManager": "npm@10.5.0",
  "engines": {
    "node": ">=18.0.0 <19.0.0"
  }
}
